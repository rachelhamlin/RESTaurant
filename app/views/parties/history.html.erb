<%= stylesheet_link_tag   'loggedin', media: 'all' %>

<header class='navbar'>
  <div id="restaurant">Taqueria</div>
  <div id="employee">
    <%= @employee.name.capitalize %> |
    <% if @employee.is_admin %><%= 'Manager' %><% else %><%= 'Server' %><% end %> at
    <%= @employee.restaurant_location.split(/ |\_/).map(&:capitalize).join(" ") %> |
    <%= link_to "Home", profile_path %>
  </div>
</header><br>

<div class='content'>

  <div class='six columns offset-by-three'>
    <div class='menu'>

      <h5>Past Parties</h5>
        <ul>
          <% @paidparties.each do |party| %>

            <li>
              Table <%= party.table_number %>, party of <%= party.number_of_people %> (<%= party.created_at.strftime("%d %b. %Y") %>)
            </li>

          <% end %>
        </ul>

        <%= button_to "Back", profile_path, {method: 'get'} %>

  </div>
</div>
