<%= form_tag order_path, method: :post do %>
​
  <% @desserts.each do |dessert| %>
    <%= label_tag dessert[:name]  %>
    <%= hidden_field_tag "order[desserts][][id]", dessert[:id] %>
    <%= number_field_tag "order[desserts][][quantity]"  %>
  <% end %>
​
  <%= submit_tag "Add" %>
​
<% end %>
